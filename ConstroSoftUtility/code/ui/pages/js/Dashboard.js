$(document).ready(function () {
    //alert("Init");
    initBootstrapComponants();
});

function initBootstrapComponants() {

    var dropDownIds = ["drpPropertyName"];
    applySelectPicker(dropDownIds);
}
